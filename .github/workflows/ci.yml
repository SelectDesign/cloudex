name: Elixir CI

on:
  pull_request:
    types: [synchronize, opened, reopened, ready_for_review]

env:
  MIX_ENV: test
  CI: true

jobs:
  test:
    runs-on: ubuntu-24.04
    if: github.event.pull_request.draft == false
    steps:
      - uses: actions/checkout@v3
      - name: Set up Elixir
        uses: erlef/setup-beam@v1
        with:
          elixir-version: "1.15.8" # Define the elixir version [required]
          otp-version: "25.3.2.15" # Define the OTP version [required]
      - name: Restore dependencies cache
        uses: actions/cache@v4
        with:
          path: deps
          key: ${{ runner.os }}-mix-${{ hashFiles('**/mix.lock') }}
          restore-keys: ${{ runner.os }}-mix-
      - name: Restore build cache
        uses: actions/cache@v4
        with:
          path: _build
          key: ${{ runner.os }}-mix-build-${{ hashFiles('**/mix.lock') }}
          restore-keys: ${{ runner.os }}-mix-build-
      - name: Install dependencies
        run: mix deps.get
      - name: Check format
        run: mix format --check-formatted
      - name: Compile dependencies
        run: mix deps.compile
      - name: Compile app
        run: mix compile --warnings-as-errors
      # This project has existing credo issues that need to be resolved before this can run in CI.
      #- name: Run credo
      #  run: mix credo
      - name: Run tests
        run: mix test
